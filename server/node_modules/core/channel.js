class Channel {
	constructor() {
		this._name = null;
		this._users = [];
		this._allowed_groups = [];
	};
	
	/**
	 * @param {string} var1
	 */
	setName(var1) {
		this._name = var1;
	};

	/**
	 * @param {User} var1
	 */
	addUser(var1) {
		this._users.push(var1);
	};

	/**
	 * @param {Array.<User>} var1
	 */
	setUsers(var1) {
		this._users = var1;
	};

	/**
	 * @param {Group} var1
	 */
	setAllowedGroups(var1) {
		this._allowed_groups = var1;
	};

	getName() {
		return this._name;
	};

	getUser(var1) {
		let users = this._users;

		for(let i = 0; i < users.length; i++) {
			let user = users[i];

			if(user.getUsername() === var1) {
				return user;
			}
		};
	};

	getUsers() {
		return this._users;
	};

	getAllowedGroups() {
		return this._allowed_groups;
	};

	notify(var1) {
		let users = this._users;

		for(let i = 0; i < users.length; i++) {
			let user = users[i];

			user.notify(var1);
		};
	};

	/**
	 * @param {} var1
	 */
	static addChannel(var1) {

	};

	/**
	 * @param {string} var1
	 */
	static getChannel(var1) {

	}
};